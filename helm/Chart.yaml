apiVersion: v2
name: siprec-stack
description: >
  streamlink SIPREC media-recorder DaemonSets (one node-pinned pod per
  configured recorder node, host-network, each fronted directly by its own
  GKE Internal Passthrough NLB).
type: application
version: 0.3.0
appVersion: "1.0.0"
keywords:
  - siprec
  - sip
  - recording
  - rtp
maintainers:
  - name: platform-team
