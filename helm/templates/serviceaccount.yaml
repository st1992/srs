apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "siprec.serviceAccountName" . }}
  namespace: {{ include "siprec-stack.namespace" . }}
  labels:
    {{- include "siprec.labels" . | nindent 4 }}
  {{- if .Values.siprecRecorder.gcp.workloadIdentity.gcpServiceAccount }}
  annotations:
    # Workload Identity binding — links this Kubernetes ServiceAccount to the
    # GCP IAM Service Account so that pods can call GCS without a key file.
    iam.gke.io/gcp-service-account: {{ .Values.siprecRecorder.gcp.workloadIdentity.gcpServiceAccount | quote }}
  {{- end }}
